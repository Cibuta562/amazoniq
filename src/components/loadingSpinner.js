import React from 'react';
import "./loadingSpinner.css"

const LoadingSpinner = () => {
    return (
        <div className="loading-container">
            <div className="spinner"></div>
            {/* Add more styling or elements if needed */}
        </div>
    );
};

export default LoadingSpinner;
